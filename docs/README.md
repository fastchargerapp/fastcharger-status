## Переключение между режимами экрана обслуживания (MaintenanceScreen)
Подготовлены шаблоны для следующих ситуаций:

- [.default.template.status.json](.default.template.status.json) - по умолчанию, для обновления устаревших версий, поддержка или обратная совместимость которых не возможна
- [.system_failure.template.status.json](.system_failure.template.status.json) - в случае возникновения экстренной ситуации и нужно предупредить пользователей, чтобы подождали пока мы решаем проблему

Далее можно скопировать содержимое `{}.template.status.json` (см выше) и вставить в файл для нужного окружения:

- production > [status.json](status.json)
- staging > [status-stg.json](status-stg.json)

Сделать коммит и отправить изменения в [git-репозитории](https://github.com/fastchargerapp/fastcharger-status). Коммит можно делать из редактора github

- [редактор production](https://github.com/fastchargerapp/fastcharger-status/edit/main/docs/status.json)
- [редактор staging](https://github.com/fastchargerapp/fastcharger-status/edit/main/docs/status-stg.json)

## Информация по схеме MaintenanceStatus

Для получения актуальной информации лучше обращаться [openapi/maintenance.json](https://github.com/fastchargerapp/fastcharger-mobileapp/blob/develop/packages/api/assets/schemes/openapi/maintenance.json)

В целях чтобы любой разработчик или нетехнический специалист мог понять, как работает система управления экраном обслуживания, не изучая OpenAPI-схему напрямую, ниже приводится краткое описание свойств.

Основное свойство `maintenance` определяет, включен ли режим обслуживания:
- `true` — приложение покажет полноэкранный экран с информацией из `fullscreen`
- `false` — приложение работает в обычном режиме, пользователь может пользоваться им как обычно

Дополнительно можно указать свойство `version`, чтобы ограничить действие режима обслуживания определёнными версиями приложения:
- Пример: `"version": "<=25.08.25"` — правило будет действовать только для версий приложения, которые меньше или равны указанной дате
- Если `version` не указан или равен `null`, правило применяется ко всем версиям приложения

Это позволяет гибко управлять поведением:
- Показывать обновление только устаревшим версиям
- Отключать функционал для старых версий, которые больше не поддерживаются

### Управление экраном MaintenanceStatus.fullscreen

Свойство `fullscreen` определяет, что будет показано пользователю на экране во время режима обслуживания. Оно обязательно и содержит следующие элементы:

### Детальная настройка MaintenanceStatus.fullscreen

#### fullscreen.title
Заголовок, который будет отображаться в верхней части экрана. Пример:
```json
"title": "Scheduled maintenance"
```

#### fullscreen.message
Основной текст сообщения, объясняющий пользователю причину режима обслуживания. Пример:
```json
"message": "We’re upgrading our system to make experience even better⚡ Everything will be back online soon — please, try opening the app again in 8 minutes 🙏⏳"
```

#### fullscreen.button.text
Текст на кнопке. Возможные варианты:
- `"text": "Update"` — призывает обновить приложение
- `"text": "Refresh"` — предлагает обновить состояние приложения
- Важно: текст должен соответствовать действию!

#### fullscreen.button.style
Стиль (цвет) кнопки. Определяет её визуальное выделение:
- `"style": "primary"` — яркая, основная кнопка (привлекает внимание)
- `"style": "secondary"` — приглушённая, второстепенная кнопка

#### fullscreen.button.link
Ссылка, на которую перейдёт пользователь при нажатии:
- `"link": ""` — пустое значение: при нажатии будет обновляться состояние приложения
- `"link": "https://onelink.to/9jqr6t"` — универсальная ссылка на магазины приложений (App Store/Google Play)
- `"link": "https://testcharger.com/feedback.html"` — ссылка на форму обратной связи

> 💡 **Совет**: Если `link` пустой, кнопка работает как "Обновить". Если указан внешний URL — произойдёт переход вне приложения.

## Рекомендации

- Используйте `version` для точечного управления — не тревожьте пользователей с актуальными версиями.
- В `message` указывайте примерное время ожидания — это снижает тревожность.
- Для экстренных ситуаций используйте `.system_failure.template.status.json`.
- Для плановых обновлений — настройте `version` и текст с призывом к обновлению.
